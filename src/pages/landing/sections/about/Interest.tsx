import React from 'react';
import { Row } from 'react-bootstrap';
import SectionTitle from '../../../../components/section/SectionTitle';

const Interest = () => {
  return (
    <Row>
      <SectionTitle header="Interests" />
    </Row>
  );
};

export default Interest;
